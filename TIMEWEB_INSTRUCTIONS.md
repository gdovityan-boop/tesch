
# 🚀 Инструкция по развертыванию на Timeweb (Shared Hosting)

Так как у вас общий хостинг (не VPS), мы будем использовать панель управления Timeweb для настройки Node.js.

## ЭТАП 1: Подготовка на ПК

1. **Скачайте архив** с этим проектом к себе на компьютер.
2. Распакуйте его.
3. Откройте терминал в папке проекта и выполните сборку фронтенда:
   ```bash
   npm install
   npm run build
   ```
   *Это создаст папку `dist`. Она критически важна.*

4. **Подготовьте файлы для загрузки:**
   Вам нужно будет загрузить на хостинг СЛЕДУЮЩИЕ файлы и папки (можно упаковать их в zip архив `deploy.zip`):
   *   Папка `dist` (целиком)
   *   Файл `server.js`
   *   Файл `package.json`
   *   Файл `db_setup.sql` (для импорта в базу)

   *(Папку `node_modules` загружать НЕ НУЖНО, мы установим её на сервере через терминал, это быстрее).*

## ЭТАП 2: Настройка в панели Timeweb

1. **Создание Базы Данных:**
   *   Зайдите в Панель управления -> **Базы данных**.
   *   Нажмите "Создать базу данных".
   *   Придумайте имя (например, `u12345_tech`) и пароль. **Запишите их!**
   *   Нажмите на значок **phpMyAdmin** рядом с новой базой.
   *   В phpMyAdmin выберите вашу базу слева, перейдите во вкладку "Импорт", выберите файл `db_setup.sql` и нажмите "Вперед". Таблицы создадутся.

2. **Настройка Сайта:**
   *   Зайдите в **Сайты**.
   *   Привяжите ваш домен к директории (например, `/public_html`).
   *   ВАЖНО: В настройках сайта выберите обработчик **Node.js** (обычно там можно выбрать версию, ставьте 18 или 20).

## ЭТАП 3: Загрузка и Запуск

1. **Загрузка файлов:**
   *   Зайдите в **Файловый менеджер**.
   *   Перейдите в папку вашего сайта (куда привязан домен).
   *   Удалите там всё лишнее (index.php и т.д.).
   *   Загрузите ваш архив `deploy.zip` и распакуйте его там.
   *   Убедитесь, что `server.js` и папка `dist` лежат в корне сайта.

2. **Настройка .env:**
   *   В файловом менеджере создайте файл `.env`.
   *   Вставьте туда настройки (замените данные на свои из шага 2.1):
     ```env
     PORT=3000
     DB_HOST=localhost
     DB_USER=u12345_tech
     DB_PASSWORD=ваш_пароль
     DB_NAME=u12345_tech
     DB_PORT=3306
     ```
     *(Примечание: на Timeweb порт обычно управляется автоматически через Passenger, но лучше оставить переменную).*

3. **Установка зависимостей (SSH):**
   *   На главной странице панели Timeweb найдите "Доступ по SSH" и включите его.
   *   Откройте **Консоль** (терминал) прямо в браузере (значок `>_` вверху справа).
   *   Перейдите в папку сайта:
     ```bash
     cd public_html
     ```
   *   Установите библиотеки:
     ```bash
     npm install --production
     ```

4. **Перезагрузка:**
   *   Вернитесь в раздел "Сайты".
   *   Нажмите на шестеренку или настройки Node.js.
   *   Нажмите **"Перезапустить приложение"**.

---

## 🛠️ КАК ОБНОВЛЯТЬ САЙТ (ИЗМЕНЯТЬ РЕКВИЗИТЫ)

Если вам нужно сменить кошелек, текст или картинки:

1. **На компьютере:**
   Откройте файл `src/services/mockData.ts` (или другие файлы).
   Внесите изменения (например, замените `usdtTrc20Address`).
   Сохраните файл.

2. **Пересборка:**
   Откройте терминал на ПК в папке проекта и выполните:
   ```bash
   npm run build
   ```
   *Это обновит папку `dist`.*

3. **Загрузка:**
   Зайдите в Файловый менеджер Timeweb.
   Удалите старую папку `dist`.
   Загрузите новую папку `dist` с вашего ПК.

4. **Перезапуск:**
   В разделе "Сайты" нажмите **"Перезапустить приложение"**.

Готово! Изменения появятся на сайте.
